import { Identifiers } from "@angular/compiler";

export interface User {
  id: number;
  userName: string;
  name: string;
  password: string;
  dateOfBirthday: Date;
  lastname: string;
  personalIdNumber: string;
  roleId: number;
}
